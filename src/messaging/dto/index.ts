/**
 * DTO exports - simplified for MVP
 * 
 * Note: Validation is handled at gateway level using manual checks
 * since we're using raw WebSocket messages.
 */

export {}; // Empty - validation done inline in gateway
