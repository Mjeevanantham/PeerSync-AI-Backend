# Server Configuration
NODE_ENV=development
PORT=3000

# ═══════════════════════════════════════════════════════════════════════════════
# SUPABASE CONFIGURATION (REQUIRED)
# ═══════════════════════════════════════════════════════════════════════════════
# Get these from your Supabase project settings:
# https://supabase.com/dashboard/project/_/settings/api

# Your Supabase project URL
SUPABASE_URL=https://your-project-ref.supabase.co

# Supabase anonymous key (public, safe to expose in frontend)
# Found in: Project Settings > API > anon public
SUPABASE_ANON_KEY=eyJhbGci...your-anon-key

# Supabase service role key (secret, server-side only)
# Used for admin operations like user sync
# Found in: Project Settings > API > service_role (keep this secret!)
SUPABASE_SERVICE_ROLE_KEY=eyJhbGci...your-service-role-key

# Supabase JWT secret (from Settings > API > JWT Settings)
# Used to verify JWT tokens locally (HS256)
# Found in: Project Settings > API > JWT Settings > JWT Secret
SUPABASE_JWT_SECRET=your-jwt-secret-string

# Database URL (for migrations only - get from Settings > Database > Connection string)
# DATABASE_URL=postgresql://postgres.[project-ref]:[PASSWORD]@aws-0-[region].pooler.supabase.com:6543/postgres

# ═══════════════════════════════════════════════════════════════════════════════

# WebSocket Configuration
WS_PORT=3001
WS_PATH=/ws
WS_HEARTBEAT_INTERVAL=30000
WS_CONNECTION_TIMEOUT=60000

# Session Configuration
SESSION_TIMEOUT=3600000
MAX_SESSIONS_PER_USER=5

# Logging
LOG_LEVEL=debug

# CORS Configuration (comma-separated origins)
# CORS_ORIGIN=http://localhost:3000,http://localhost:5173
